# Pydantic - Structured outputs (not in core packages)
pydantic==2.11.7
pydantic-settings==2.10.1
pydantic_core==2.33.2

# LangChain ecosystem (not in core packages)
langchain==0.3.26
langchain-community==0.3.27
langchain-core==0.3.68
langchain-text-splitters==0.3.8
langsmith==0.4.4

# LangGraph (not in core packages)
langgraph==0.5.1
langgraph-checkpoint==2.1.0
langgraph-prebuilt==0.5.2
langgraph-sdk==0.1.72

# Snowflake (not in core packages)
# Note: pandas is provided by Dataiku core packages, so we don't need [pandas] extra
# snowflake package provides snowflake.core module (Root class)
# Using version that matches working configuration in other projects
snowflake==1.0.5
snowflake-connector-python>=3.0.0
snowflake-snowpark-python>=1.10.0

# Typing extensions (might be in core, but keeping for compatibility)
typing_extensions==4.14.1
